{
  "name": "haveniq-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test --test-concurrency=1",
    "_test_note": "concurrency 1 because the parallel runner loses a random file. Node 24.13.1 drops one test file's results per run with 'Unable to deserialize cloned data', reporting it as a generic failure. Measured: parallel gives 1421/1422/1427/1428 tests and a phantom failure in a different file each time; serial gives 1428 pass 0 fail, repeatably. A lost file's real failures are lost with it, and the commit gate depends on this number. 31s to 61s locally, nothing in CI. Use 'node --test' directly for a fast local sweep.",
    "audit:high": "npm audit --omit=dev --audit-level=high",
    "audit:weekly": "npm audit --audit-level=moderate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "@sentry/node": "^10.53.1",
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.5.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.3.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "otplib": "^12.0.1",
    "pg": "^8.20.0",
    "plaid": "^42.2.0",
    "posthog-node": "^5.35.1",
    "resend": "^6.12.0",
    "socket.io": "^4.8.3",
    "stripe": "^17.7.0",
    "uuid": "^13.0.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  }
}
